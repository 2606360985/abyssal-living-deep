# ABYSS//MINER — 当前功能测试指南

本指南涵盖项目已实现的所有可测试功能。项目包含两个独立体验：**ABYSS//MINER**（默认工业 ROV 深海原型）和 **ABYSSAL — The Living Deep**（遗留程序化海洋）。

---

## 1. 环境准备

**要求：** Node.js 20.19+ 或 22.12+，支持 WebGL2 的桌面浏览器，硬件加速显卡。

```sh
npm ci
npm run dev
```

打开 `http://localhost:5173/` 即进入 ABYSS//MINER。
在 URL 后加 `?experience=legacy` 进入遗留海洋体验。

---

## 2. ABYSS//MINER（默认体验）

### 2.1 ROV 运动控制

| 按键 | 功能 |
| --- | --- |
| W / S | 前进 / 后退（带惯性） |
| A / D | 左转 / 右转（缓慢偏航） |
| Q / E | 下降 / 上升 |
| Z / X | 左右侧移 |
| Shift | 自由镜头模式下加速 |

**测试要点：**
- 松键后应有短暂滑行，快速反向输入先减速再反向
- 推进器叶片转速随推力变化
- 机体有轻微俯仰/横滚摆动
- 贴近海床（高度 < 5m）时下向推进器搅起泥沙，停止后数秒消散

### 2.2 相机模式

| 按键 | 功能 |
| --- | --- |
| F1 | 隐藏 / 显示全部 HUD |
| F2 | 显示 / 隐藏性能信息面板 |
| F3 | 切换自由相机（拖拽视角，WASD/QE 移动，Shift 加速） |
| F4 | 切换电影相机（自动环绕运镜） |
| 2 | 重置到深海抵达初始位 |

**测试要点：**
- 跟随模式相机有弹簧阻尼，不会追尾穿地
- 自由相机可独立移动，ROV 逻辑继续运行
- 电影相机做缓慢环绕运动
- 相机有海床碰撞修正，不会被近景岩石长期遮挡

### 2.3 声呐系统（Space 触发）

| 按键 | 功能 |
| --- | --- |
| Space | 发射主动声呐 |

**测试要点：**
- 发射后可见世界空间波前沿海床扩张
- 波前被地形深度正确遮挡，不穿透前景岩石
- 约 0.65 秒后 HUD 显示回波数据：方位(BEARING)、距离(RANGE)、估算尺寸、置信度、序列号
- 冷却时间 4 秒，长按不重复触发
- 第一次扫描固定发现 205m 处的 `UNKNOWN CONTACT`
- 扫描矿区时显示 `POLYMETALLIC NODULE FIELD`，置信度 HIGH

### 2.4 多金属结核矿区

**测试要点：**
- ROV 接近矿区中心约 18m 时自动进入矿区状态，HUD 显示 `POLYMETALLIC NODULE FIELD`
- 矿区有约 3,000 个结核实例（3 种低模球状几何），近处有色泽差异
- 声呐扫描矿区后 HUD 分步显示：分类 → 置信度 → 成分(Ni/Co/Cu/Mn) → 估值
- 7 个可交互采样结核，靠近 4m 内且在视锥内时显示 `SAMPLE CANDIDATE` 角标

### 2.5 机械臂采样

| 操作 | 功能 |
| --- | --- |
| 鼠标左键（LMB） | 确认目标 / 启动采样 |

**测试要点：**
- 瞄准结核并左键确认后显示成分分析面板
- 再次左键启动不可中断的采样动画（约 6.6 秒）
- 动画阶段：`aim → extend → approach → close → lift → retract → stow`
- 两骨 IK 求解肩肘，腕部对准结核法线
- 爪闭合时结核从实例池隐藏，生成附着于爪的 mesh
- 接触点释放泥沙脉冲 + 金属撞击声 + 轻微镜头反馈
- 采样完成后 HUD 显示 `SAMPLE ACQUIRED` + 质量/成分/估值
- 超出臂工作范围时短促拒绝音 + 提示信息

### 2.6 阶段切换（数字键 1–7）

| 按键 | 阶段 |
| --- | --- |
| 1 | descent（下潜，未实现演出） |
| 2 | arrival（深海抵达，默认） |
| 3 | sonar（声呐） |
| 4 | nodules（矿区） |
| 5 | sampling（采样） |
| 6 | contact（未知目标，未实现） |
| 7 | lost（信号丢失，未实现） |

**测试要点：**
- 每个阶段重置 ROV、镜头、HUD、音频和目标状态
- 阶段幂等：反复切换无残留对象、音频泄漏或内存上涨
- 阶段 6 和 7 目前为空壳，无实际演出内容

### 2.7 音频系统

| 按键 | 功能 |
| --- | --- |
| M | 静音 / 取消静音 |

**测试要点：**
- 首次键鼠/点击交互后解锁音频（浏览器自动播放限制）
- 深海底噪（低频棕噪声）持续播放
- 推进器频率随推力变化
- 声呐 PING 为短正弦扫频 + 延迟回波 + 低频尾音
- 确认/拒绝/撞击各有独立音效
- 静音后不创建持续振荡节点
- 切换标签页时音频自动暂停

### 2.8 HUD 与性能

**测试要点：**
- HUD 显示：航向罗盘、深度/高度读数、推力百分比、状态文字
- F2 性能面板显示：GPU 型号、分辨率、中位/P95 帧时间、Draw Calls、三角形数、阶段、音频状态
- 深度读数基于 3,800m 任务基准
- HUD 隐藏后画面完全干净

### 2.9 URL 参数测试

| 参数 | 效果 |
| --- | --- |
| `?pose=hero` | 英雄镜头位（默认） |
| `?pose=close` | 近景位 |
| `?pose=low` | 低角度位 |
| `?hud=0` | 隐藏 HUD |
| `?still=1` | 冻结模拟和相机，用于截图对比 |
| `?camera=cinematic` | 电影相机 |
| `?perf=1` | 显示性能面板 |
| `?quality=high\|medium\|low` | 画质档位 |

可组合使用，例如：`?still=1&hud=0&pose=hero` 用于固定帧对比。

### 2.10 开发调试

在浏览器控制台中使用 `window.__miner`：
- `window.__miner.setPose('hero'|'close'|'low')` — 重置位置、时间、输入和相机到固定位
- `window.__miner.director.goto('sonar')` — 跳转阶段
- `window.__miner.state` — 查看当前游戏状态
- `window.__miner.stats` — 查看帧时间统计

---

## 3. 遗留海洋体验（`?experience=legacy`）

### 3.1 栖息地导航

| 按键 | 功能 |
| --- | --- |
| 1–4 | 旅行到珊瑚礁 / 海带林 / 蓝水 / 深海 |
| Ascend / Descend | 连续上升 / 下潜 |
| 深度停靠点 | 200m / 600m / 1,000m / 热泉花园 |
| Explore / C | 打开栖息地图表，选择目的地旅行 |
| G / R | 打开世界实验室 / 生成新种子 |
| O / J | 观察野生动物 / 打开野外日志 |

### 3.2 程序化控制面板

在 World Lab 中可调整：
- **World 标签：** 地形起伏、生物覆盖度、海带高度、栖息地尺度
- **Life 标签：** 动物丰度、捕食者、底栖生物、水母
- **Water 标签：** 水清度、洋流、生物发光、潜水灯、上升流
- **Weather 标签：** 太阳高度、云量、风速、浪高、风暴强度 + 更多天气参数

### 3.3 可复现 URL

```
?experience=legacy&site=reef&seed=713
?experience=legacy&site=deep&seed=82017&glow=1.5
?experience=legacy&site=reef&seed=713&depth=600&predators=1.4&jellies=0
```

---

## 4. 自动化测试

### 4.1 逻辑模拟套件

```sh
npm test
```

依次运行 6 个模拟脚本，覆盖：
- 质量模拟（`quality-sim.mjs`）
- 水下地形与旅程（`underwater-sim.mjs`）
- 连通海洋模拟（`connected-ocean-sim.mjs`）
- 动物群落（`fauna-sim.mjs`）
- 动物运动（`animal-motion-sim.mjs`）
- 生物群落（`biome-sim.mjs`）

### 4.2 冒烟测试

```sh
npm run smoke
```

### 4.3 Miner QA（需要 Chromium）

```sh
npm run qa:miner
```

使用 Puppeteer 驱动硬件加速 Chromium，运行交互检查并采集 60 秒 1920×1080 帧时间样本。验证：
- HUD/性能切换、自由相机分离与返回、电影相机、前进运动、输入释放、最小海床间隙、阶段重置、窗口缩放
- 检查浏览器 console 错误/警告
- 截图和 JSON 报告写入 `tools/shots/`（Git 忽略）

### 4.4 构建验证

```sh
npm run build
npm run preview
```

---

## 5. 已知限制

- **阶段 1（下潜）、6（未知目标）、7（信号丢失）** 尚未实现实际演出内容
- 岩石碰撞有意省略，自由探索可穿过单块岩石
- 系绳为静态曲面上网格，不模拟水面船只
- 运动有惯性和阻尼但非流体力学模拟
- WebGPU 未启用
- 移动端性能不保证
